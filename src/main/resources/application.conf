ktor {
    deployment {
        port = 8080
        port = 8080
    }
    application {
        modules = [br.com.ApplicationKt.module]
    }
}
jwt {
    domain = "https://jwt-provider-domain/"
    audience = "jwt-audience"
    realm = "ktor sample app"
}
